#pragma once

// quick sort
void quickSort(int inputCollection[], int low, int high);

//function that finds the most frequent element in a sorted array
int mostFrequentElement(const int array[], int length);
